import styled from 'styled-components';

const Button = styled.button`
  padding: 5px 10px;
  margin-bottom: 5px;
  border-radius: 13px;

  color: black;

  cursor: pointer;
`;

export default Button;
